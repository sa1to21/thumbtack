# Thumbtack Auto-Responder - Mac Setup Guide

Пошаговая инструкция для установки и запуска бота на Mac.

## Системные требования

- macOS 10.15 (Catalina) или новее
- Python 3.8 или новее
- Chrome браузер установлен
- Mac должен быть включен 24/7 (или хотя бы в рабочие часы)

## Быстрый старт

### 1. Получить Gmail App Password

1. Включи 2-Step Verification (2FA) в Google аккаунте:
   - Перейди на https://myaccount.google.com/security
   - Найди "2-Step Verification" и включи

2. Создай App Password:
   - Перейди на https://myaccount.google.com/apppasswords
   - Выбери "Mail" и "Mac"
   - Скопируй сгенерированный 16-значный пароль

### 2. Установка

```bash
# Скопируй проект на Mac отца
cd ~/Desktop
# (загрузи папку thumbtack)

cd thumbtack

# Запусти установку
chmod +x setup_mac.sh
./setup_mac.sh
```

Скрипт автоматически:
- Проверит Python
- Создаст виртуальное окружение
- Установит все зависимости

### 3. Настройка

Скопируй и отредактируй конфиг:

```bash
cp .env.example .env
nano .env
```

Заполни:
```env
GMAIL_EMAIL=hammerandhewllc@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
DEFAULT_MESSAGE=Hi! Thanks for reaching out. I'm available for this project. Could you share more details?
```

Сохрани (Ctrl+O, Enter, Ctrl+X).

### 4. Первый запуск (логин в Thumbtack)

**Важно:** При первом запуске нужно вручную залогиниться в Thumbtack.

```bash
source venv/bin/activate
python main.py
```

Откроется окно Chrome:
1. Залогинься в Thumbtack вручную
2. Не закрывай бота - подожди пока он проверит статус
3. Если все ОК, увидишь "✅ Logged in successfully"
4. Нажми Ctrl+C чтобы остановить

**Сессия сохранена!** Теперь бот может работать автоматически.

### 5. Установка автозапуска

Чтобы бот запускался при старте Mac и работал постоянно:

```bash
chmod +x install_service_mac.sh
./install_service_mac.sh
```

Готово! Бот теперь работает в фоне.

## Управление ботом

### Проверить статус

```bash
launchctl list | grep thumbtack
```

Если видишь число (PID) - бот работает.

### Просмотр логов

```bash
# Основной лог бота
tail -f logs/bot.log

# Системные логи (stdout/stderr)
tail -f logs/stdout.log
tail -f logs/stderr.log
```

### Перезапуск

```bash
launchctl kickstart -k gui/$(id -u)/com.thumbtack.autoresponder
```

### Остановка

```bash
launchctl unload ~/Library/LaunchAgents/com.thumbtack.autoresponder.plist
```

### Запуск

```bash
launchctl load ~/Library/LaunchAgents/com.thumbtack.autoresponder.plist
```

### Удаление автозапуска

```bash
./uninstall_service_mac.sh
```

## Как это работает на Mac

1. **LaunchAgent** запускает бота при логине пользователя
2. **Chrome окно** открывается, но его можно минимизировать или скрыть
3. **Email Monitor** слушает Gmail через IMAP IDLE (мгновенные уведомления)
4. При новом лиде:
   - Ждет 30-50 секунд (имитация человека)
   - Открывает inbox в уже запущенном Chrome
   - Находит непрочитанное сообщение
   - Печатает ответ посимвольно с задержками
   - Отправляет

**IP адрес** - родной домашний, никаких VPN/прокси не нужно!

## Настройки поведения

Все настраивается через `.env`:

### Изменить время реакции (секунды)

```env
REACTION_TIME_MIN=25
REACTION_TIME_MAX=60
```

### Изменить текст ответа

```env
DEFAULT_MESSAGE=Your custom message here
```

### Изменить скорость печати

```env
TYPING_SPEED_MIN=0.03  # быстрее
TYPING_SPEED_MAX=0.20  # медленнее
```

### Интервал перезапуска браузера

```env
BROWSER_RESTART_INTERVAL=10800  # 3 часа
```

## Troubleshooting

### Бот не запускается

Проверь логи:
```bash
cat logs/stderr.log
```

### "Not logged in to Thumbtack"

Запусти вручную и залогинься заново:
```bash
source venv/bin/activate
python main.py
```

### Chrome не открывается

Проверь что Chrome установлен:
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --version
```

### "No module named 'imap_tools'"

Переустанови зависимости:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Gmail не подключается

1. Проверь что App Password правильный (16 символов без пробелов)
2. Проверь что 2FA включена
3. Проверь что IMAP включен в Gmail settings

### Бот отвечает слишком быстро/медленно

Отредактируй `.env`:
```bash
nano .env
# Измени REACTION_TIME_MIN и REACTION_TIME_MAX
```

Перезапусти:
```bash
launchctl kickstart -k gui/$(id -u)/com.thumbtack.autoresponder
```

## Безопасность

✅ **Домашний IP** - Thumbtack видит родной IP провайдера
✅ **Undetected Chrome** - обход anti-bot защиты
✅ **Не headless** - настоящее окно браузера (не палится)
✅ **Человеческие задержки** - случайное время реакции и печати
✅ **Session persistence** - не логинишься каждый раз

## Мониторинг

Рекомендую раз в день проверять:

```bash
# Статус
launchctl list | grep thumbtack

# Свежие логи (последние 50 строк)
tail -50 logs/bot.log
```

Или можешь добавить уведомления в Telegram (расширение для будущего).

## Что дальше?

После успешного запуска можно добавить:
- Разные шаблоны ответов по типу проекта
- Уведомления в Telegram о новых лидах
- Дашборд со статистикой
- Расписание работы (отвечать только в рабочие часы)

## Вопросы?

Если что-то не работает, проверь:
1. Логи: `tail -f logs/bot.log`
2. Статус сервиса: `launchctl list | grep thumbtack`
3. Chrome процесс: `ps aux | grep chrome`

Удачи! 🚀
